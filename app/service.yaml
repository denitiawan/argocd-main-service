apiVersion: v1
kind: Service
metadata:
  labels:
    app: main-service
  name: main-service
  namespace: my-application
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8001
  selector:
    app: main-service
  type: ClusterIP
